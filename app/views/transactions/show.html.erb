<p style="color: green"><%= notice %></p>

<div>
<%= link_to "Back", category_transactions_path(@transaction_cat_id), class:"btn btn-sucess mb-3 bg-primary fs-4 text-white" %>
</div>
<div id="transactions">
  <% @transactions.each do |transaction| %>
    <%= render partial: "single_transaction", locals: { transaction: transaction } %>
  <% end %>
</div>
