<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>

    <p class="link-underline-light">
<%= link_to category_transactions_path(category) do %>
    <%= render partial: "category", locals: { category: category } %>
  <% end %>
    </p>
  <% end %>
</div>

<%= link_to "add a new category", new_category_path, class:"btn btn-sucess mb-3",style:"background-color: #cc6600;" %>
